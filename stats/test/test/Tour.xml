<StatsConfig>
    <!-- Seasons -->
    <Pair Key="CareerBaseSeason" Value="2017" />
    <Pair Key="CurrentSeason" Value="2022" />

    <!-- Database -->
    <Pair Key="ClearDb" Value="true" />
    <Pair Key="DbDir" Value="{stats_root}/test/database/Tour" />

    <!-- Static -->
    <Pair Key="StaticDir" Value="{stats_root}/test/static/Tour" />

    <!-- Input -->
    <Pair Key="InputMapperName" Value="file" />
    <Pair Key="InputMapperDir" Value="{stats_root}/test/input/Tour" />

    <!-- Output -->
    <Pair Key="OutputDir" Value="{stats_root}/test/result/Tour" />
</StatsConfig>
